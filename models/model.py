class Model(object):
    @classmethod
    def post(path: str, params: dict, authorize: bool = False) -> dict:
        return {
            'id': 'id',
            'type': 'type',
            'stat': 1,
            'name': 'name',
        }
